# Legacy Archive

这个目录用于存放已经退出主运行链、但暂时保留以便回查的历史文件。

## 归档目的

1. 避免旧模块、旧组件、旧测试页继续污染当前工程判断。
2. 保留历史实现，方便后续对照迁移。
3. 让 `src/` 主链只保留当前真实运行版本。

## 当前已归档内容

- 旧页面与备份入口
- 旧模块系统
- 旧 UI 组件集合
- 旧加载页、引导页、Toast
- 旧数据源
- 旧测试页

## 规则

- 不要从 `legacy/` 重新直接引用代码回到主链。
- 如需复用历史实现，应先人工审查，再按当前数据模型与样式规范重写。
